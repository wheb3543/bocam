# تقرير المرحلة الأولى: فحص وتحليل تخطيط لوحة الإدارة

## حالة المرحلة

- المرحلة: المرحلة الأولى، الفحص والتحليل.
- الحالة: مكتملة من ناحية التحليل والتوثيق.
- نطاق التنفيذ: توثيق وتحليل فقط.
- كود التطبيق المعدل: لا يوجد.
- الملف الثابت للخطة: [RESTRUCTURING_PLAN.md](RESTRUCTURING_PLAN.md).

## الخلاصة التنفيذية

المشكلة الأساسية في التنقل من الشريط الجانبي القديم سببها استخدام `window.location.href` داخل `DashboardSidebarV2`. هذا يؤدي إلى تحميل كامل للمتصفح، وبالتالي فقدان شجرة React وحالة التبويبات وإعادة إنشاء الواجهة بالكامل.

يوجد سبب معماري ثانوي يحتاج إلى المعالجة في المرحلة التالية: `App.tsx` يملك مسارين منفصلين للإدارة، أحدهما `/admin` والآخر `/admin/*`، وكلاهما يغلف المحتوى داخل `DashboardShell`. الانتقال بين الفرعين قد يعيد تركيب الـ shell، لذلك يجب توحيد نقطة تركيب layout الإداري.

أما الانتقال بين صفحات مختلفة داخل `/admin/*` فيستخدم `wouter` في المسارات نفسها، لكن الصفحات الداخلية تتبدل داخل `Switch`. لذلك يجب الحفاظ على `DashboardShell` في موضع ثابت وجعل منطقة المحتوى هي الجزء المتغير فقط.

## خريطة العرض الحالية

```text
App
└── Router
    └── Switch
        ├── Route /admin
        │   └── Suspense
        │       └── DashboardShell
        │           ├── AdminTopNavigation
        │           ├── AdminTabs
        │           └── AdminDashboard
        └── Route /admin/*
            └── Suspense
                └── DashboardShell
                    ├── AdminTopNavigation
                    ├── AdminTabs
                    └── Switch
                        └── صفحة الإدارة الحالية
```

## المكونات الحالية ومسؤولياتها

### `DashboardShell`

المسار: `client/src/components/layout/DashboardShell.tsx`

المسؤوليات الحالية:

- التحقق من حالة المصادقة.
- عرض حالة التحميل.
- عرض واجهة تسجيل الدخول عند غياب المستخدم.
- قراءة المسار الحالي من `wouter`.
- تشغيل `useAdminTabs`.
- عرض `AdminTopNavigation`.
- عرض `AdminTabs`.
- عرض محتوى الصفحة.

الملاحظة المعمارية:

المكوّن يجمع مسؤوليات المصادقة، والـ shell، وحالة التبويبات، وعرض التنقل. سيحتاج في المرحلة التالية إلى فصل أو تثبيت هذه المسؤوليات دون كسر سلوك المصادقة.

### `AdminTopNavigation`

المسار: `client/src/components/layout/AdminTopNavigation.tsx`

المسؤوليات الحالية:

- تصفية الأقسام حسب الصلاحيات والميزات.
- تحديد القسم والقسم الفرعي النشطين.
- التنقل باستخدام `setLocation`.
- عرض الشريط الإداري العلوي وقوائمه المنسدلة.

القرار:

سيتم إخراجه من shell الإداري في مرحلة إعادة الشريط الجانبي. لا يتم حذف `TopNavbar` العام تلقائيًا؛ فهو مكوّن مختلف تستخدمه بعض صفحات `DashboardLayout`.

### `AdminTabs`

المسار: `client/src/components/layout/AdminTabs.tsx`

المسؤولية الحالية:

- عرض التبويبات.
- تفعيل التبويب عبر `setLocation`.
- إغلاق التبويب عبر callback صادر من الأب.

الملاحظة:

هو مكوّن مستقل بصريًا، لكن حالته ومصدر حياته مرتبطان بـ `DashboardShell` من خلال `useAdminTabs`. في المرحلة التالية يجب تثبيت مكانه مع الـ shell الموحد، لا إعادة كتابته من الصفر.

### `useAdminTabs`

المسار: `client/src/hooks/layout/useAdminTabs.ts`

المسؤوليات الحالية:

- تصفية الصفحات القابلة للوصول.
- مطابقة المسار مع سجل التنقل المركزي.
- استعادة التبويبات من `localStorage`.
- إزالة التكرار والمسارات القديمة.
- إضافة المسار الحالي إلى التبويبات.
- إغلاق التبويب وتحديد fallback.

الملاحظة:

المنطق الحالي يدعم المتطلبات الأساسية، لكن إعادة تركيب `DashboardShell` تعني إعادة تشغيل hook وحالته. سيعاد تقييم دورة الحياة في مرحلة فصل layout والتبويبات.

### `DashboardSidebarV2`

المسار: `client/src/components/layout/DashboardSidebarV2.tsx`

المسؤوليات الحالية:

- تصفية عناصر الشريط حسب الصلاحيات.
- تصفية عناصر الشريط حسب الدور وصندوق البريد الموحد.
- حفظ العناصر المرئية في `localStorage`.
- عرض الشريط المكتبي وشريط الهاتف.
- عرض كل الأدوات وتحرير الشريط.
- عرض الشارات والتنبيهات.
- تسجيل العناصر المستخدمة مؤخرًا.

المشكلة المؤكدة:

```ts
window.location.href = href;
```

هذا السطر يسبب تحميلًا كاملًا للمتصفح عند التنقل الداخلي، وهو السبب المباشر لفقدان حالة التبويبات وإعادة إنشاء التطبيق.

### `DesktopSidebar` و`MobileBottomNav`

المسارات:

- `client/src/components/layout/sidebar/DesktopSidebar.tsx`
- `client/src/components/layout/sidebar/MobileBottomNav.tsx`

المسؤولية:

- عرض عناصر التنقل فقط واستدعاء `handleNavClick` من الأب.

القرار:

لا حاجة لإعادة كتابة هذين المكونين. التغيير الأساسي سيكون في callback المرسل من `DashboardSidebarV2`.

### `useSidebarNavigation`

المسار: `client/src/hooks/layout/useSidebarNavigation.ts`

الملاحظة:

يوجد hook آخر لإدارة الشريط يعتمد على مصدر التنقل المركزي ويستخدم `useLocation`. يجب تحديد لاحقًا هل هو مسار بديل أم مستخدم في `DashboardSidebar` مختلف. لا يجوز دمج مسؤولياته مع `DashboardSidebarV2` دون قياس أثر واضح.

## مصادر التنقل والصلاحيات

المصدر المركزي:

- `client/src/config/sidebarNavigation.ts`

طبقة التوافق:

- `client/src/components/layout/sidebarData.ts`

الموجود حاليًا:

- تعريفات `NavItem` و`NavGroup`.
- روابط الإدارة.
- متطلبات الميزات.
- متطلبات الصلاحيات.
- الأدوار المسموحة.
- العناصر الافتراضية.

القرار:

يجب استخدام نفس المصدر المركزي للشريط الجانبي والتبويبات. لا يتم إنشاء سجل روابط جديد أثناء إعادة الهيكلة.

## مسارات التنقل المكتشفة

### مسار الإدارة الرئيسي

```text
/admin
```

يعرض `AdminDashboard` داخل `DashboardShell`.

### مسارات الإدارة الفرعية

```text
/admin/*
```

تعرض صفحات الإدارة داخل `Switch` مستقل داخل `DashboardShell`.

المخاطر:

- وجود فرعين Route منفصلين قد يؤدي إلى إعادة تركيب shell عند الانتقال بين الصفحة الرئيسية وصفحة فرعية.
- يجب توحيد تركيب الـ shell في المرحلة الثانية.

## الاختبارات الحالية

### اختبارات التبويبات

المسار: `client/src/hooks/layout/useAdminTabs.test.ts`

تغطي:

- مطابقة المسار مع عنصر التنقل.
- إزالة التكرار.
- إزالة المسارات القديمة.
- إبقاء الصفحة الرئيسية أول تبويب.
- رفض المسارات غير المعروفة.
- إغلاق التبويب واختيار fallback.

### اختبارات تخطيط الإدارة

المسار: `client/src/__tests__/adminLayoutVerification.test.ts`

الحالة الحالية:

- مصممة للتحقق من شريط التنقل العلوي.
- تتحقق من عدم تركيب الشريط الجانبي القديم.
- تتحقق من القوائم المتداخلة في الشريط العلوي.

النتيجة:

هذه الاختبارات ستحتاج إلى مراجعة في المرحلة الخاصة بإرجاع الشريط الجانبي، لأنها تثبت السلوك القديم المؤقت وليس السلوك المستهدف النهائي.

### اختبارات بيانات الشريط

المسار: `client/src/components/layout/sidebarData.test.ts`

تتحقق من:

- ظهور صندوق البريد الموحد.
- وجود سياسة الأدوار للعناصر المحمية.
- توافق البيانات مع مصدر التنقل.

## قرارات المرحلة الأولى

1. الشريط الجانبي المستهدف هو `DashboardSidebarV2` ومكوناته الحالية.
2. لن يتم إنشاء شريط جانبي جديد.
3. يجب إزالة `window.location.href` من التنقل الداخلي للشريط في مرحلة التنفيذ المناسبة.
4. سيتم استخدام `setLocation` من `wouter` للتنقل الداخلي.
5. سيتم إبقاء `AdminTabs` كمكوّن مستقل للعرض، مع تثبيت حالته في layout إداري موحد.
6. سيتم إلغاء استخدام `AdminTopNavigation` من shell الإداري.
7. لن يتم حذف `TopNavbar` العام قبل التحقق من الصفحات التي تستخدم `DashboardLayout`.
8. سيتم توحيد فرعي `/admin` و`/admin/*` ضمن تركيب layout ثابت في المرحلة الثانية.
9. لن يتم تغيير مصدر الصلاحيات أو تعريفات التنقل المركزية.
10. لن يتم تعديل قاعدة البيانات أو API ضمن هذا المسار.

## نطاق المرحلة التالية

بعد اعتماد هذا التقرير، تبدأ المرحلة الثانية فقط وتشمل:

- تصميم layout إداري ثابت.
- توحيد تركيب routes الإدارية.
- تحديد موضع `DashboardSidebarV2` و`AdminTabs`.
- إبقاء منطقة المحتوى هي الجزء المتغير.
- إضافة الاختبار الأولي لعدم إعادة التحميل الكامل.

## معايير قبول المرحلة الأولى

- تم تحديد السبب المباشر لإعادة التحميل.
- تم تحديد السبب المعماري المحتمل لإعادة تركيب shell.
- تم تحديد مكونات الشريط الجانبي السابق ومسؤولياتها.
- تم تحديد حالة التبويبات ومصدرها.
- تم تحديد الاختبارات المتأثرة.
- تم تحديد نطاق المرحلة الثانية دون تنفيذها.
- لم يتم تعديل كود التطبيق.
