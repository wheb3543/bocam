/**
 * @deprecated Re-export bridge for backward compatibility.
 * Prefer importing directly from "@core/hooks/useSSE".
 */
export * from '@core/hooks/useSSE';
export { default } from '@core/hooks/useSSE';
