# Project TODO

- [x] إصلاح فشل Docker production install بسبب تشغيل prepare/Husky مع تثبيت production dependencies
- [x] إعادة بناء المشروع والتحقق من نجاح فحص TypeScript والبناء والتشغيل
- [x] حفظ checkpoint جديد جاهز لإعادة النشر
- [x] تشغيل `pnpm run check` والتحقق من سلامة الأنواع
- [x] التحقق من تشغيل الخادم والجاهزية الإنتاجية (تشغيل تجريبي مؤقت)
- [x] توفير/التحقق من `WHATSAPP_WEBHOOK_VERIFY_TOKEN` أو التعامل مع وضع الويبهوك الاختياري
- [x] التحقق من توافق وضع التفعيل (Activation mode) لملف الترخيص `license.json`
- [x] التأكد من إقلاع الخادم واستعداده لربط المنفذ الإنتاجي
- [x] فحص رسالة «الميزة غير مفعلة» وتحديد متطلبات الترخيص الصالح
- [x] التحقق من توقيع ملف license.json ودمج الملف ومفتاح التحقق في صورة الإنتاج
- [x] تهيئة معرّف ترخيص ثابت يطابق المعرّف الموقّع داخل license.json
- [x] اختبار تفعيل جميع الميزات من خلال الترخيص الصالح
- [x] اختبار استجابة الخادم الإنتاجي على المنفذ 3000 عبر نقطة الصحة
- [x] إزالة قيود تكبير الشاشة وإضافة أسماء وصول وصفية للروابط وفق تقرير Lighthouse
- [x] تحسين تقسيم حزم JavaScript وفصل مكتبات الجداول والتصدير إلى حزمة مؤجلة
- [x] تحسين تحميل خط Cairo للمسار الحرج على الهواتف وإضافة preload آمن
- [x] إضافة معالجة وضغط تلقائي للصور المرفوعة من لوحة التحكم وتحويلها إلى صيغة AVIF الحديثة مع آلية fallback
- [x] توصيل سجل صور المحتوى الحالي بمسار الرفع والتخزين لإنشاء مكتبة وسائط موحدة للصور المرفوعة والمستخدمة في قاعدة البيانات فقط
- [x] تطوير رفع متعدد الملفات مع تحويل AVIF وتسجيل بيانات كل ملف في المكتبة
- [x] إنشاء صفحة مكتبة وسائط إدارية تدعم العرض والبحث والتصفية والاختيار
- [x] إضافة محدد وسائط موحد بخياري «رفع ملف» و«اختيار من مكتبة الوسائط» لحقول الصور الإدارية
- [x] اختبار الرفع المتعدد والاختيار من المكتبة وتوثيق النتيجة في checkpoint
- [x] إنشاء خدمة خادمية لتحويل الصور إلى AVIF أو WebP واستخراج أبعادها وفهرستها عند الرفع
- [x] إضافة endpoint آمن للرفع المتعدد حتى 20 صورة وفهرسة نتائج كل عملية رفع
- [x] ربط BulkImageUpload وContentManagementPage فعلياً مع /api/upload/batch ثم تحديث قائمة الصور
- [x] إزالة ضغط JPEG المحلي من BulkImageUpload والاعتماد على التحويل الخادمي إلى AVIF
- [x] استبعاد سجلات الصور ذات deletedAt من عمليات قائمة المكتبة وإحصاءاتها
- [x] تحديث ImageUploadDialog القديم ليستخدم الرفع الخادمي وخيار اختيار من مكتبة الوسائط
- [x] تدقيق حقول الملفات الإدارية والتأكد من أن حقول الصور تستخدم تدفق مكتبة الوسائط الموحد

- [x] إصلاح فشل صفحة مكتبة الوسائط عند غياب جدول images في قاعدة بيانات الإنتاج
- [x] التحقق من إنشاء جدول images وإعادة اختبار استعلام المكتبة
- [x] حفظ checkpoint لإصلاح جدول images

- [x] إصلاح فشل صفحة مكتبة الوسائط عند غياب جدول images في قاعدة بيانات الإنتاج
- [x] التحقق من إنشاء جدول images وإعادة اختبار استعلام المكتبة
- [x] حفظ checkpoint لإصلاح جدول images
- [x] إضافة استثناء آمن لرسائل checkpoint المولّدة تلقائياً مع إبقاء فحص commitlint للـ commits العادية
- [x] تدقيق جداول images وmedia ومسارات الرفع الحالية لاختيار مصدر بيانات موحّد للوسائط
- [x] اعتماد نموذج مكتبة وسائط موحّد يدعم الصور والفيديو والصوت والمستندات مع بيانات النوع والحجم والمعاينة
- [x] توسيع الرفع والتخزين للتحقق من أنواع الوسائط وحدودها ومعالجة الصور فقط إلى AVIF
- [x] تطوير واجهة مكتبة الوسائط لتعرض معاينات ملائمة لكل نوع وتدعم البحث والتصفية والمجلدات والإجراءات الجماعية
- [ ] اختبار الأنواع المدعومة وحفظ checkpoint لمكتبة الوسائط الشاملة
