# نتائج اختبار حمولات Meta التجريبية

**التاريخ:** 18 أغسطس 2026  
**النطاق:** صندوق البريد الموحد في SGH CRM  
**طريقة الاختبار:** اختبارات Vitest مع محاكاة معزولة لقاعدة البيانات، وبناء إنتاج ناجح. لم تُضف بيانات تجريبية إلى قاعدة البيانات المنشورة أثناء هذا التحقق.

## ما تم اختباره

تستند العينات إلى بنى Webhooks الخاصة بـ Messenger وInstagram وFacebook، مع استخدام معرفات ومحتوى اصطناعيين حصراً. كل سجل مخصص للتجربة يستخدم بادئة `sgh-meta-test-`، ويعرضه الصندوق بشارة **بيانات اختبار** كي لا يختلط بمراسلات العملاء الفعلية. [1] [2] [3]

| السيناريو | النتيجة | ما تم التحقق منه |
|---|---:|---|
| رسالة Messenger نصية | ناجح | المرسل، المستلم، المعرّف، التوقيت، النص، واتجاه الرسالة |
| رسالة Messenger بثلاثة مرفقات | ناجح مع فجوة موثقة | حفظ رابط أول مرفق وإبقاء المرفقات المتبقية في الحمولة الخام |
| Instagram Direct مع `reply_to` | ناجح | حفظ معرّف الرسالة الأم كسياق للرد |
| تعليق Instagram متداخل | ناجح | الكاتب و`parent_id` وسياق الوسيط/المنشور |
| تعليق Facebook مضاف | ناجح | النص والكاتب وسياق المنشور ورابطه الأصلي |
| تعديل تعليق Facebook (`verb=edit`) | فجوة مقصودة ومثبتة | لا يُطبّع ولا ينشئ سجلاً؛ يحتاج دعماً منفصلاً للتحديثات |
| دفعة Messenger متعددة الأحداث | ناجح | تطبيع وتغذية عنصرين مستقلين من حمولة واحدة |

## النتائج الآلية

| طبقة التحقق | الاختبارات الناجحة | النتيجة |
|---|---:|---|
| تطبيع الحمولات الرسمية | 6 | جميع السيناريوهات المتوقعة، بما فيها الدفعة وفجوة التعديل |
| إدخال قاعدة البيانات المحاكى | 5 | إنشاء السياق والعنصر، منع التكرار، وتنظيف الحسابات الموسومة فقط |
| واجهة صندوق البريد | 5 | شارة بيانات الاختبار، مرجع الرد، ورابط المرفق |
| الإجمالي المركز | 16 | ناجح |

كما نجح `pnpm run check` دون أخطاء، ونجح `pnpm run build` لإنتاج نسخة قابلة للنشر. نجحت مجموعة الاختبارات المركزة لتكامل Meta بالكامل (**16/16**). أما تنفيذ Vitest الكامل فقد أنجز **925 اختباراً ناجحاً من أصل 933**؛ والإخفاقات الثمانية المتبقية سابقة وغير مرتبطة بهذه الإضافة، وتتوزع على اختبار رخصة لا يتوافق مع واجهة الشبكة في البيئة، وخمسة اختبارات لمسارات المخيمات تفترض سلوكاً مختلفاً عند غياب قاعدة البيانات، واختبارَي تحقق لطول حقول SEO يحتويان قيماً لا تطابق حدود الاختبار نفسها.

## تجربة الواجهة المنشورة

بعد نشر النسخة الجديدة، يفتح المسؤول صفحة **إعدادات ربط Meta** ثم يستخدم زر **إدخال بيانات الاختبار**. يمر الزر بالمسار ذاته المستخدم لتطبيع وتخزين أحداث Webhook، وينشئ سبعة أحداث مدعومة موزعة على Messenger وInstagram وFacebook. سيظهر في صندوق البريد وسم واضح لكل محادثة تجريبية، ويمكن إزالة جميع السجلات التي تبدأ بحسابات `sgh-meta-test-` عبر زر **تنظيف بيانات الاختبار**. لا يمس التنظيف أي حساب أو حدث لا يحمل هذه البادئة.

> لا يرسل هذا الإجراء طلبات إلى Meta ولا يتطلب الأسرار الفعلية للحسابات؛ إنه يتعامل فقط مع حمولة محلية مطابقة للبنية المتوقعة لاختبار العرض والتخزين.

## فجوات تم رصدها بوضوح

ينبغي قبل اكتمال الاستفادة الكاملة تطوير نموذج مرفقات متعدد بدلاً من حفظ أول رابط فقط، وإضافة معالجة `verb=edit` والحذف والتفاعلات. تحتفظ الحمولات الخام بهذه البيانات للتدقيق اللاحق، لكن الواجهة الحالية لا تحوّلها إلى تحديثات مرئية.

## المراجع

[1] [Meta — Webhooks for Messenger](https://developers.facebook.com/docs/messenger-platform/webhooks/)

[2] [Meta — Instagram Webhooks](https://developers.facebook.com/docs/instagram-platform/webhooks/)

[3] [Meta — Page Webhooks](https://developers.facebook.com/docs/graph-api/webhooks/reference/page/)
