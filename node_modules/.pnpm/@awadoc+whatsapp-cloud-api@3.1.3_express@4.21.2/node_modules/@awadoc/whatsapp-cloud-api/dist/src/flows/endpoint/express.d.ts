import { Router } from 'express';
import { type FlowEndpointOptions } from './types';
export declare function createFlowEndpoint(options: FlowEndpointOptions): Router;
export type { FlowEndpointOptions, FlowRequest, FlowRequestHandler, FlowErrorHandler, } from './types';
export { FlowResponseBuilder } from './types';
