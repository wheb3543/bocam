import type { NavigateActionSchema, DataExchangeActionSchema, CompleteActionSchema, OpenUrlActionSchema, DynamicRef } from '../types';
export declare class NavigateAction {
    private readonly screenName;
    private readonly payload?;
    constructor(screenName: string, payload?: Record<string, unknown>);
    toJSON(): NavigateActionSchema;
}
export declare class DataExchangeAction {
    private readonly payload?;
    constructor(payload?: Record<string, unknown>);
    toJSON(): DataExchangeActionSchema;
}
export declare class CompleteAction {
    private readonly payload?;
    constructor(payload?: Record<string, unknown>);
    toJSON(): CompleteActionSchema;
}
export declare class OpenUrlAction {
    private readonly url;
    constructor(url: string | DynamicRef);
    toJSON(): OpenUrlActionSchema;
}
export type Action = NavigateAction | DataExchangeAction | CompleteAction | OpenUrlAction;
