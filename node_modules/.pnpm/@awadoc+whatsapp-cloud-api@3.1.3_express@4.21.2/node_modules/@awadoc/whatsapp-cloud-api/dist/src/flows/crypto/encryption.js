"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.validateSignature = exports.generateKeyPair = exports.encryptResponse = exports.decryptRequest = void 0;
var crypto_1 = __importDefault(require("crypto"));
var AES_ALGORITHM = 'aes-128-gcm';
var AUTH_TAG_LENGTH = 16;
function decryptRequest(encryptedRequest, privateKeyPem) {
    var encrypted_flow_data = encryptedRequest.encrypted_flow_data, encrypted_aes_key = encryptedRequest.encrypted_aes_key, initial_vector = encryptedRequest.initial_vector;
    var flowData = Buffer.from(encrypted_flow_data, 'base64');
    var iv = Buffer.from(initial_vector, 'base64');
    var encryptedKey = Buffer.from(encrypted_aes_key, 'base64');
    var aesKey = crypto_1.default.privateDecrypt({
        key: privateKeyPem,
        padding: crypto_1.default.constants.RSA_PKCS1_OAEP_PADDING,
        oaepHash: 'sha256',
    }, Uint8Array.from(encryptedKey));
    var encryptedBody = flowData.subarray(0, -AUTH_TAG_LENGTH);
    var authTag = flowData.subarray(-AUTH_TAG_LENGTH);
    var decipher = crypto_1.default.createDecipheriv(AES_ALGORITHM, Uint8Array.from(aesKey), Uint8Array.from(iv));
    decipher.setAuthTag(Uint8Array.from(authTag));
    var decrypted = Buffer.concat([
        decipher.update(Uint8Array.from(encryptedBody)),
        decipher.final(),
    ]);
    var decryptedData = JSON.parse(decrypted.toString('utf-8'));
    return {
        decryptedData: decryptedData,
        aesKey: aesKey,
        iv: iv,
    };
}
exports.decryptRequest = decryptRequest;
function encryptResponse(response, aesKey, iv) {
    var flippedIv = Buffer.alloc(iv.length);
    for (var i = 0; i < iv.length; i += 1) {
        flippedIv[i] = iv[i] ^ 0xff;
    }
    var cipher = crypto_1.default.createCipheriv(AES_ALGORITHM, Uint8Array.from(aesKey), Uint8Array.from(flippedIv));
    var encrypted = Buffer.concat([
        cipher.update(JSON.stringify(response), 'utf-8'),
        cipher.final(),
        cipher.getAuthTag(),
    ]);
    return encrypted.toString('base64');
}
exports.encryptResponse = encryptResponse;
function generateKeyPair() {
    var _a = crypto_1.default.generateKeyPairSync('rsa', {
        modulusLength: 2048,
        publicKeyEncoding: {
            type: 'spki',
            format: 'pem',
        },
        privateKeyEncoding: {
            type: 'pkcs8',
            format: 'pem',
        },
    }), privateKey = _a.privateKey, publicKey = _a.publicKey;
    return { privateKey: privateKey, publicKey: publicKey };
}
exports.generateKeyPair = generateKeyPair;
function validateSignature(payload, signature, appSecret) {
    if (!signature || !signature.startsWith('sha256=')) {
        return false;
    }
    var expectedSignature = signature.slice(7);
    var computedSignature = crypto_1.default
        .createHmac('sha256', appSecret)
        .update(payload)
        .digest('hex');
    return crypto_1.default.timingSafeEqual(Buffer.from(expectedSignature), Buffer.from(computedSignature));
}
exports.validateSignature = validateSignature;
//# sourceMappingURL=encryption.js.map