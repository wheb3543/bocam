"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
Object.defineProperty(exports, "__esModule", { value: true });
var logger_1 = require("../src/utils/logger");
describe('DebugLogger', function () {
    var originalEnv = process.env;
    var consoleLogSpy;
    var consoleDirSpy;
    beforeEach(function () {
        jest.resetModules();
        process.env = __assign({}, originalEnv);
        consoleLogSpy = jest.spyOn(console, 'log').mockImplementation(function () { });
        consoleDirSpy = jest.spyOn(console, 'dir').mockImplementation(function () { });
    });
    afterEach(function () {
        process.env = originalEnv;
        jest.restoreAllMocks();
    });
    it('should not log when env var is not set', function () {
        delete process.env.DEBUG_WHATSAPP_CLOUD_API;
        logger_1.DebugLogger.logIncomingWebhook({});
        expect(consoleLogSpy).not.toHaveBeenCalled();
    });
    it('should log when env var is "true"', function () {
        process.env.DEBUG_WHATSAPP_CLOUD_API = 'true';
        var payload = { test: 'data' };
        logger_1.DebugLogger.logIncomingWebhook(payload);
        expect(consoleLogSpy).toHaveBeenCalledWith(expect.stringContaining('[Incoming Webhook]'));
        expect(consoleDirSpy).toHaveBeenCalledWith(payload, expect.anything());
    });
    it('should log outgoing requests', function () {
        process.env.DEBUG_WHATSAPP_CLOUD_API = 'true';
        var req = { method: 'post', url: '/test', data: { foo: 'bar' } };
        logger_1.DebugLogger.logOutgoingRequest(req);
        expect(consoleLogSpy).toHaveBeenCalledWith(expect.stringContaining('[Outgoing Request] POST /test'));
        expect(consoleDirSpy).toHaveBeenCalledWith(req.data, expect.anything());
    });
});
//# sourceMappingURL=logger.test.js.map