# تنفيذ صفحة النشر متعددة المنصات

**التاريخ:** 18 أغسطس 2026  
**المسار:** `/admin/content/publishing`

## ما أصبح متاحاً

أصبحت صفحة النشر مساحة عمل عربية متجاوبة لإنشاء **مسودة مركزية**، واختيار Facebook وInstagram وX وLinkedIn وYouTube وTikTok، واختيار وسائط حقيقية من مكتبة الوسائط، ثم تمرير المسودة إلى الموافقة والجدولة. تُنشأ وجهة مستقلة لكل منصة كي لا يحجب خطأ وجهة واحدة بقية الوجهات عند تفعيل النشر الحي.

| الطبقة | التنفيذ |
|---|---|
| قاعدة البيانات | جداول للحسابات، والمسودات، ومراجع الوسائط، ووجهات النشر، وسجل المحاولات. تتضمن كل وجهة مفتاح idempotency، وكل مسودة مجدولة تقبل معرف مهمة Heartbeat. |
| الخادم | راوتر `content.publishing` للمسودة والتعديل والموافقة والرفض والجدولة والإلغاء، بصلاحيات تقيّد المراجعة بالأدوار الإشرافية. |
| الواجهة | محرر RTL، بطاقات منصات مع مؤشر الاتصال، منتقي من مكتبة الوسائط، لوحة مسودات وحالات وجهات، وموافقة/إرجاع/جدولة. |
| التنفيذ الدوري | مسار `/api/scheduled/social-publish` لا يقبل إلا هوية Heartbeat ويتعرف إلى المنشورات فقط بمعرف المهمة الموثوق. يقفل المنشور قبل التنفيذ لمنع التكرار. |
| الحماية | لا تنتقل توكنات المنصات إلى المتصفح، ولا تنفذ الواجهة نشرًا خارجياً قبل وجود حساب OAuth متصل وموافقة وإعداد محول المنصة. |

## ما لم يُفعّل عمداً بعد

لا تتصل الصفحة حالياً بحسابات خارجية أو تنشر أي محتوى فعلياً؛ إذ لا توجد OAuth credentials أو حسابات منصات مصرح بها في المشروع. لذلك تحفظ الجدولة والوجهات، ويعيد معالج Heartbeat أي منشور مستحق إلى حالة قابلة للتنفيذ بدلاً من إصدار طلب خارجي من دون تفويض. هذه حماية مقصودة وليست محاكاة للنشر.

## نتيجة التحقق

اجتازت الاختبارات المركزة **8/8**، وتشمل واجهة اختيار المنصات والوسائط وإنشاء المسودة، وصلاحيات راوتر النشر، ومعالج Heartbeat الموثوق. نجح `pnpm run check` ونجح `pnpm run build`.

نفّذت مجموعة Vitest الكاملة **952** اختباراً؛ نجح **944** وبقيت **8** إخفاقات سابقة وغير مرتبطة بصفحة النشر، في اختبارات الترخيص، ومسارات المخيمات عند غياب قاعدة البيانات، وقيم حدود SEO غير الصحيحة داخل الاختبار نفسه.

## تفعيل النشر الحي بعد النشر

بعد نشر النسخة، تُنشأ مهمة Heartbeat دورية على المسار `/api/scheduled/social-publish` ثم يُخزن `task_uid` في الإعداد `social_publish_dispatch_task_uid`. بعدها تحصل كل مسودة جديدة على المعرف ذاته، ويتعرف المعالج إلى المنشورات المستحقة من هذا المعرف فقط.

يحتاج كل محول منصة قبل تفعيله إلى OAuth أو توكن تطبيق صالح وحساب مملوك وموافقة API مناسبة. تبدأ المرحلة العملية بـ Meta باستخدام إعدادات الربط الموجودة، ثم X وLinkedIn وYouTube وTikTok عبر OAuth. ويجب تنفيذ أول نشر حي بحساب اختبار ومنشور غير حساس مع مراجعة سجل المحاولات.

> لا ينبغي إدخال أي توكن داخل المتصفح أو ضمن الكود. تُضاف الأسرار عبر إعدادات المشروع، وتُحفظ refresh tokens مشفرة في خادم التطبيق عند تطوير موصل OAuth لكل منصة.
