# تقرير تحقق صندوق البريد الموحد

## نطاق التحقق

تم التحقق من صفحة صندوق البريد الموحد وطبقة tRPC ودوال قاعدة البيانات وتعريفات التبويبات بعد إضافة دعم Messenger وInstagram وFacebook وX وLinkedIn وYouTube، مع فصل الرسائل عن التعليقات وإضافة اختبارات RTL للحالات الأساسية.

## اختبارات الميزة

| المجموعة | النتيجة |
| --- | --- |
| `client/src/pages/admin/communications/socialInboxConfig.test.ts` | 4 اختبارات ناجحة |
| `client/src/pages/admin/communications/MessagesPage.test.tsx` | 4 اختبارات ناجحة |
| `server/socialInbox.test.ts` | 8 اختبارات ناجحة |
| إجمالي اختبارات صندوق البريد | 16 اختباراً ناجحاً |
| `pnpm run check` | ناجح |
| `pnpm run build` | ناجح |

تغطي الاختبارات التبويبات المطلوبة، فصل تعليقات Facebook بفلاتر مستقلة، البحث مع إزالة المسافات، الحالة الفارغة، اختيار المحادثة وتعليمها كمقروءة، إنشاء حساب منصة، التحقق من المنصة غير المدعومة، تحديث حالة المزامنة، وترحيل طفرات القراءة والتمييز والتعيين إلى طبقة البيانات.

## نتيجة مجموعة الاختبارات الكاملة

تم تشغيل `pnpm test` بعد التعديلات. نجحت 898 من أصل 906 اختبارات، ونجحت جميع اختبارات صندوق البريد الجديدة. بقيت 8 إخفاقات في 3 ملفات موجودة مسبقاً ولا تستورد صفحة صندوق البريد ولا تعتمد على جداولها الجديدة:

| الملف | السبب الظاهر في السجل |
| --- | --- |
| `server/_core/__tests__/license.test.ts` | اختبار `getHardwareId` يتوقع خطأ عند غياب واجهة شبكة صالحة، بينما بيئة الاختبار الحالية تعيد قيمة بديلة من معلومات الشبكة المتاحة. |
| `server/routers/__tests__/camps.test.ts` | خمس حالات تتوقع قوائم فارغة عند غياب قاعدة البيانات، بينما `ensureDatabaseAvailable` يرفع `TRPCError` صريحاً قبل تنفيذ الإجراء. |
| `client/src/pages/admin/content/__tests__/contentManagement.test.ts` | قيم fixtures الخاصة بعنوان ووصف SEO أقصر من الحدود التي يختبرها الملف؛ الإخفاقات تتعلق بطول النصوص (`24 < 30` و`50 < 120`). |

هذه الإخفاقات مستقلة عن ملفات صندوق البريد الموحد، ولذلك لم يتم تغيير سلوك الترخيص أو المخيمات أو اختبارات SEO ضمن هذه المهمة لتجنب تعديل نطاق غير مطلوب. يلزم إصلاحها في مهمة صيانة منفصلة إذا كان المطلوب جعل كامل مستودع الاختبارات أخضر.
